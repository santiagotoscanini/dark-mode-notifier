// swift-tools-version:4.0
import PackageDescription

let package = Package(
    name: "dark-mode-notifier",
    dependencies: [],
    targets: [
    	.target(
            name: "dark-mode-notifier",
            path: "."
        )
    ]
)
